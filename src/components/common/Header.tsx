'use client';
import MainNavBar from './MainNavBar';

function Header() {
    return (
        <header className='sticky top-0 z-10 w-full'>
            <MainNavBar />
        </header>
    );
}

export default Header;
